# CLAUDE.md — Claude Code Instructions

See README.md for full project documentation.

## Claude-Specific Notes
- Use `python` (not `python3`) to run scripts
- Conda env is `SPEOC` — use `conda run -n SPEOC python ...` or `conda activate SPEOC`
- `PYTHONPATH=.` assumed — import project modules from root
- Code conventions: see QUALITY.md
- Long-term improvement items: see LONGTERM.md
